package com.eventplanner.config;

// NOTE: This duplicate SecurityConfig is disabled to avoid conflicts with
// com.eventplanner.security.SecurityConfig, which is the primary security configuration.
// If re-enabling is needed, rename the class and ensure bean names do not clash.
class SecurityConfigDisabled {
    // intentionally empty
}
